import React from "react";
import { Card } from "./Card";

export default {
  title: "Molecules/Card",
  component: Card,
};

const Template = () => <Card />;

export const Primary = Template.bind({});
