import React from "react";
import { LinkedCard } from "./linked-card";

export default {
  title: "Molecules/Linked Card",
  component: LinkedCard,
};

const Template = () => <LinkedCard />;

export const Primary = Template.bind({});
