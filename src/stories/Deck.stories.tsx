import React from "react";
import { Deck } from "./Deck";

export default {
  title: "Organisms/Deck",
  component: Deck,
};

const Template = () => <Deck />;

export const Primary = Template.bind({});